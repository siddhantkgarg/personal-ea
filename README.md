# Personal EA Android

Android-first personal assistant MVP. Current build captures notification events locally on-device and shows them in a simple activity dashboard.

## Privacy
- No credentials are committed.
- Captured notification text is stored locally on the device.
- This repository is public, so do not add API keys or secrets directly to source control.
